# Server
PORT=3001
NODE_ENV=production

# Database (replace with Render Postgres connection string)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Redis (replace with Render Redis connection string, only if using queue/worker)
REDIS_URL=redis://default:PASSWORD@HOST:6379
